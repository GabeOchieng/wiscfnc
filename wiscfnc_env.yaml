name: wiscfnc
dependencies:
  - python=3.5
  - smart_open
  - scikit-learn=0.18.1
  - gensim
  - ipython
  - pandas

# environment setup for conda environment
# also requires installation of keras (with backend of tensorflow): 
#install tensorflow: $ conda install -c conda-forge tensorflow
#install keras: $ pip install git+git://github.com/fchollet/keras.git --upgrade --no-deps
# install nltk: $ pip install nltk
